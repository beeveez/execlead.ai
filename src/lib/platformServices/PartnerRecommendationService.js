export function nextBestAction(partner) { if (partner.nextAction) return { action: partner.nextAction, basis: 'Recorded next action' }; if (partner.partnerStatus === 'At Risk') return { action: 'Schedule an executive relationship review', basis: 'Partner status is At Risk' }; if (partner.partnershipStage === 'Negotiation') return { action: 'Confirm agreement owners and decision date', basis: 'Partnership is in Negotiation' }; return { action: 'Record the next relationship milestone', basis: 'No next action is currently recorded' }; }
export const PartnerRecommendationService = { nextBestAction };