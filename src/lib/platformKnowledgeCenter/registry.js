// Platform Knowledge Center™ — Living Registry
// Curated source of truth for every module, engine, route, entity, and decision.
import { ROUTES } from './routes';
import { ENTITIES } from './entities';
import { AI_ENGINES } from './engines';
import { MODULES } from './modules';
import { ENGINEERING_PHASES, ADRS, RELEASES, ARCHITECTURE_LAYERS, DEPENDENCY_EDGES } from './architecture';

export {
  ROUTES, ENTITIES, AI_ENGINES, MODULES,
  ENGINEERING_PHASES, ADRS, RELEASES, ARCHITECTURE_LAYERS, DEPENDENCY_EDGES
};

export const PK_VERSION = '1.0.0';
export const LAST_INDEXED = '2026-07-31';