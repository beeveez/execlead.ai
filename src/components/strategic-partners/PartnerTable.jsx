import React from 'react';
import { Link } from 'react-router-dom';
import { Building2, ChevronRight } from 'lucide-react';
import { categoryLabel } from '@/lib/strategic-partners/partnerConfig';
export default function PartnerTable({ partners, onEdit }) { if (!partners.length) return <div className="rounded-xl border border-dashed border-border py-14 text-center"><Building2 className="mx-auto text-muted-foreground" /><p className="mt-3 text-foreground">No strategic partners recorded</p></div>; return <div className="overflow-x-auto rounded-xl border border-border"><table className="w-full min-w-[760px] text-sm"><thead className="bg-muted/40 text-left text-xs text-muted-foreground"><tr><th className="p-3">Partner</th><th>Category</th><th>Status</th><th>Stage</th><th>Owner</th><th>Health</th><th>Actions</th></tr></thead><tbody className="divide-y divide-border">{partners.map((p) => <tr key={p.id}><td className="p-3 font-medium text-foreground">{p.partnerName}</td><td className="text-muted-foreground">{categoryLabel(p.partnerCategory)}</td><td>{p.partnerStatus}</td><td>{p.partnershipStage}</td><td>{p.relationshipOwner || 'Unassigned'}</td><td>{p.partnerHealthScore ?? '—'}</td><td><button onClick={() => onEdit(p)} className="min-h-11 px-2 text-primary">Edit</button><Link to={`/operations/strategic-partners/${p.id}`} className="inline-flex min-h-11 min-w-11 items-center justify-center" aria-label={`Open ${p.partnerName}`}><ChevronRight size={16} /></Link></td></tr>)}</tbody></table></div>; }