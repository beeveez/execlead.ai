import React from 'react';
export default function ControlChart({ metric }) {
  const span = Math.max(1, metric.upperControlLimit - metric.lowerControlLimit); const position = Math.max(0, Math.min(100, ((metric.currentValue - metric.lowerControlLimit) / span) * 100));
  return <div className="rounded-xl border border-white/5 bg-white/[0.02] p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="text-xs font-semibold text-white">{metric.metricName}</h3><p className="mt-1 text-[10px] text-white/30">LCL {metric.lowerControlLimit} · Target {metric.targetValue} · UCL {metric.upperControlLimit}</p></div><span className={`text-lg font-bold ${metric.status === 'out_of_control' ? 'text-rose-400' : metric.status === 'warning' ? 'text-amber-400' : 'text-emerald-400'}`}>{metric.currentValue}%</span></div><div className="relative mt-5 h-2 rounded-full bg-white/10"><div className="absolute inset-y-0 bg-emerald-500/25" style={{ left: `${Math.max(0, ((metric.targetValue - metric.lowerControlLimit) / span) * 100)}%`, right: 0 }} /><div className="absolute -top-1 h-4 w-1 rounded bg-white" style={{ left: `${position}%` }} /></div><div className="mt-3 flex justify-between text-[10px] text-white/30"><span>{metric.trend}</span><span>{metric.status.replaceAll('_', ' ')}</span></div></div>;
}