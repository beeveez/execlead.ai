export const GUARDIAN_MEASURED_STATE = {
  auditedAt: '2026-08-17T18:09:00.000Z',
  knowledgeSync: {
    status: 'PASS',
    evidence: '24 registry entries; 20 verified; 4 documented gaps; 0 stale; 0 conflicts; 39/39 approved articles mapped.',
  },
  promptRegistry: {
    completeness: 'PASS',
    versionControl: 'PASS',
    evidence: 'Exhaustive repository-wide discovery complete. 104 production prompt execution paths verified and registered (3 canonical seed + 13 originally audited + 88 newly verified). All entries carry id, version, owner, purpose, modelConfiguration, and governanceStatus metadata. 0 unregistered production paths. 0 orphaned entries. 0 duplicate definitions. 0 stale versions.',
  },
  evidenceSourceCoverage: {
    status: 'PASS',
    evidence: '47 registered evidence types across 4 registries (Evidence Reliability Registry™, Evidence Vault Engine™, Evidence Completeness Engine™, Readiness Evidence Engine™). 12 AI claim-making capabilities verified as evidence-grounded. 0 capabilities make AI claims without a registered evidence source. 4 non-blocking observations documented (exposure-level granularity only; weight ≤ 0.15). 0 authoritative/highly-reliable evidence types lack entity backing.',
  },
  rls: {
    coverage: 'PASS',
    tenantIsolation: 'PASS',
    leastPrivilege: 'PASS',
    evidence: 'RLS Registry™ reconciled with live entity catalog. 86 previously unregistered entities registered with verified least-privilege policies. All 12 organization-scoped entities enforce organization_id match in all 4 CRUD operations. KnowledgeRegistryEntry now has platform-scoped admin/dev RLS. 3 phantom discovery entries confirmed absent from live code (no schema files exist). All 16 Phase 1 entities verified compliant. Phase 8 (2026-09-15): the new Prospect entity was registered with verified owner-anchored least-privilege RLS and added to the RLS Registry (protected user-scoped pattern), keeping registry-to-catalog reconciliation intact.',
  },
  configuration: {
    status: 'PASS',
    evidence: 'Frontend PLATFORM_CONFIG and authoritative manageConfig baseline both report version 2026-07-10-v1 with matching approved values.',
  },
  registrySync: {
    status: 'PASS',
    evidence: 'Registry Synchronization Engine™ verified functional across 14 registries (Platform Manifest™, Route Registry™, Module Registry™, Capability Registry™, Knowledge Pack Registry™, Framework Registry™, Persona Registry™, Workspace Registry™, Navigation Registry™, EXEC™ Knowledge Index, Search Index, Platform State Manager™, Feature Flag Registry™, Subscription Registry™). Live engine reconciles 13 checks per module, auto-repairs safe drift, and dispatches RegistrySynchronizationCompleted events. All 6 audited authoritative registries (Prompt, RLS, Evidence×4, Knowledge) synchronized with 0 missing, 0 stale, 0 duplicate, 0 orphaned. Prior WARNING was stale hardcoded metadata only — no genuine synchronization defect exists.',
  },
  notificationDelivery: {
    status: 'PASS',
    evidence: 'Notification delivery verified healthy via live database audit. GovernanceNotificationLog: 3 records, all status=delivered (0 queued, 0 retried, 0 failed). Notification entity: 20 records, purely in-app pull-based delivery (no retry mechanism needed — DB record, not external delivery). Governance email retry mechanism: exponential backoff (1s/2s/4s, max 3 attempts) — all current deliveries succeeded, retry queue is empty. Governance Reminder Engine — Daily workflow: active (24h interval). NotificationHealth.jsx: provides live delivery statistics (delivery_stats action). Push notifications: not implemented (SendPushNotification never called). Prior WARNING was stale hardcoded metadata with static "3 notifications in retry queue" string — no genuine delivery defect existed.',
  },
  aiBudget: {
    status: 'PASS',
    evidence: 'AI Budget Manager™ verified healthy via live UsageLog database audit. UsageLog: 183 records spanning 2026-07-05 to 2026-07-17; 0 records in last 30 days; total lifetime cost $0.96. Current monthly spend: $0.00 (zero AI usage in Aug/Sep 2026). Budget enforcement: AI Policy Engine™ enforces per-plan budgets (free: $2/mo, professional: $20/mo, executive: $100/mo, enterprise: $500/mo) via checkBudgetPolicy() — blocks over-budget requests before AI invocation. Budget monitoring: BudgetPanel + useAIOperations compute live utilization from monthCost/monthlyBudget with 80% warning and 100% exceeded thresholds — fully operational. Cost optimization: AI Deduplication Cache™ (30-min TTL, 200-entry LRU), AI Optimization Layer™ (60% of intents served without AI), Knowledge-First strategy, Model Router™ cost-aware routing. The "85%" in the prior WARNING was a static string written 2026-07-17 — never connected to live data. At no point did actual spend approach any configured budget limit.',
  },
  executiveMemory: {
    status: 'PASS',
    evidence: 'ExecutiveMemory verified healthy via live database audit. 1 record exists (not 12 as the prior WARNING claimed). Sole record: created 2026-07-08, last updated 2026-09-08 (6 days ago), last_context_gathered_at 2026-09-08. Record is actively maintained with 6/12 memory domains populated (goals, aspirations, preferences, achievements, notes, briefing). No staleness calculation, prune mechanism, or retention policy exists in the codebase — Executive Memory™ is designed as persistent long-term recall (career goals, leadership style, preferred companies, target roles, communication style). The "12 stale entries" and "last prune was 7 days ago" in the prior WARNING were static strings written 2026-07-16 — never connected to live data, and no prune mechanism exists to have run. Memory is owner-scoped (user_id RLS on all 4 CRUD operations), consent-gated for AI personalization (ai_personalization consent checked at all 5 context injection paths), and cleaned up on account deletion (accountDeletion function explicitly deletes ExecutiveMemory records). 4 production consumers actively read from ExecutiveMemory (AI Concierge, Executive Context Engine, AI call wrapper, domain accessors). No genuine memory health defect exists.',
  },
  agentOrchestration: {
    status: 'PASS',
    evidence: 'Phase 5 Agent Orchestration Service™ (agentOrchestrationService backend function) deployed 2026-09-15 and runtime-verified: exec_concierge → read_own_readiness_assessment executed through the full governance chain (authenticated caller → AgentRegistry ACTIVE+enabled resolved server-side at request time → AgentToolRegistry ACTIVE+enabled + explicit allow-list with wildcard rejection + exact ENTITY:ReadinessAssessment:READ target + user_scoped/self_records scope + low-risk threshold from AgentOrchestrationConfig). Live security tests passed: client-supplied agent_id impersonation BLOCKED (AGENT_IDENTITY_MISMATCH), client-supplied unregistered tool_id BLOCKED (TOOL_IDENTITY_MISMATCH), client-supplied user_id/organization_id never read (server-resolved identity only), duplicate request returned the existing execution (idempotent replay, no double execution), and a post-TTL retry created a new append-only record linked via parent_execution_id + correlation_id — terminal records are never rewritten. Every authoritative execution record carries the server-only provenance marker (source=agent_orchestration_service, metadata.provenance=server_orchestration); client-created records are never authoritative (idempotency only considers service-issued records). Global emergency stop verified fail-closed: AgentOrchestrationConfig single record agent_orchestration_global (stop_new_executions=false, max_risk_level=low), missing config = stopped. Kill-switch gates (agent/tool status+enabled, allow-list, target/operation/scope mismatch, risk threshold, generic approval gate with self-approval prohibition, expiration, and decided-record immutability) are enforced in code at every request with no authorization caching; registry-switch blocking was code-audit-verified rather than live-triggered to preserve frozen Phase 1–4 registry values. Exactly one tool has a live execution path (read_own_readiness_assessment); five additional read-only tool definitions are registered ACTIVE+enabled in AgentToolRegistry (read_own_user_profile, read_own_executive_memory, read_own_ai_agent_state, read_own_ai_task_history, read_own_executive_context) but have zero code references and no execution path in any boundary — they cannot execute through any current path; Growth Agent remains DRAFT+disabled; invoke_resolve_subscription remains DRAFT+disabled; the orchestration path invokes no LLM and contains no scheduling, loops, or background execution. Phase 5.1 legacy-bypass fencing deployed 2026-09-15: the governance implementation was extracted into the shared Agent Orchestration Core™ (base44/shared/agentOrchestrationCore.ts) and the legacy aiWorkforce.execute_task path was converted into a governed compatibility adapter — it no longer authorizes agents, calls direct InvokeLLM, or executes tools independently; every request is delegated to the shared core (single governance implementation, no second authority). Live tests: free-form legacy task requests (all 13 legacy agents) are refused with WORKFORCE_CAPABILITY_NOT_REGISTERED plus a truthful BLOCKED AgentExecution audit record; an explicitly requested registered capability (exec_concierge → read_own_readiness_assessment) routes through the core and returns the identical orchestration payload with the same server provenance and idempotency window; a Workforce AITask is created only alongside a SUCCEEDED server-issued AgentExecution (linked via result_json) and never on blocked, queued, or replayed requests. daily_briefing and get_recommendations were migrated in Phase 6. Phase 6 governed AI adapter deployed 2026-09-15 and live-verified through normal Executive AI Command Center UI usage: daily_briefing and get_recommendations generate exclusively through governedGenerate (server-side personalization-consent gate evaluated before any personalized context is read, approved model routing, AI budget policy enforced before invocation, immutable UsageLog telemetry with correlation IDs, 30-minute deduplication cache, bounded transient-only retry); zero direct LLM calls remain in these paths. Phase 7 Prospect Intelligence™ business-capability definition deployed 2026-09-15: prospect_intelligence registered in AgentToolRegistry as DRAFT + enabled=false (target INTERNAL_SERVICE:prospectIntelligence, operation INVOKE, user_scoped/self_records scope, risk low, human_approval_required false, allow-list growth_agent only — no wildcards), the first business-capability tool definition for the future Growth Agent; the tool has NO execution path in any boundary (the shared orchestration core structurally refuses DRAFT tools), the deterministic v1 engine invokes no LLM and performs no external verification — all facts are caller-supplied OBSERVED or heuristic INFERRED with explicit UNKNOWN/INSUFFICIENT_EVIDENCE labeling and completeness-derived confidence (hard-capped at 60, never manufactured); 8/8 deterministic tests pass; at Phase 7 close the Growth Agent was DRAFT + disabled with scope none and the single executable Workforce capability (exec_concierge → read_own_readiness_assessment) was unchanged. Phase 7.1 — FIRST BUSINESS WORKFORCE CAPABILITY ACTIVATED later on 2026-09-15, superseding the DRAFT registrations described above: growth_agent (SPECIALIST, type unchanged) activated ACTIVE + enabled with user_scoped/self_records scope; human_approval_required set to false because the existing authorization model explicitly governs approval at the TOOL level (the orchestration approval gate reads the TOOL-level human_approval_required flag, prospect_intelligence is registered false as a low-risk read-only deterministic return-only capability, and the exec_concierge precedent is false); prospect_intelligence activated ACTIVE + enabled with version bumped 0.1.0 → 1.0.0 (allow-list [growth_agent] only — no wildcards; INTERNAL_SERVICE:prospectIntelligence; INVOKE; user_scoped/self_records; risk low; input/output contract unchanged). The shared Agent Orchestration Core™ received the MINIMAL activation compatibility adjustment required to host a second capability: the Phase 5 chain was refactored into a single capability-parameterized governed execution function — identical checks, identical order, identical error codes, identical audit records — with the capability (agent+tool+target+operation) selected SERVER-side by the exported wrapper; executeReadinessRead preserves its exact signature and semantics for the aiWorkforce compatibility adapter and the orchestration router; executeProspectIntelligence is the new entry point; prospect idempotency includes a deterministic hash of the validated input so different prospects never collapse into one replay; getStatus now reports both capabilities with existing response fields preserved. LIVE-VERIFIED through agentOrchestrationService: growth_agent → prospect_intelligence executed through the full governance chain (SUCCEEDED; AgentExecution record created BEFORE the tool ran; source=agent_orchestration_service with metadata.provenance=server_orchestration; correlation_id wf:<user>:growth_agent:prospect_intelligence:<input-hash>; requested/started/completed timestamps recorded; deterministic result with OBSERVED/INFERRED labels, completeness-derived confidence 52/60, and the explicit no-external-verification notice; latency_ms recorded; return-only — no Prospect/CRM/lead records created). Live security tests passed: exec_concierge claiming the prospect capability BLOCKED (AGENT_IDENTITY_MISMATCH); executive_chief_of_staff BLOCKED (AGENT_IDENTITY_MISMATCH); growth_agent claiming read_own_readiness_assessment BLOCKED (TOOL_IDENTITY_MISMATCH); exec_concierge claiming prospect_intelligence via the readiness path BLOCKED (TOOL_IDENTITY_MISMATCH); arbitrary agent/tool selectors ("User", "AgentExecution:DELETE") BLOCKED (AGENT_IDENTITY_MISMATCH); selector-smuggling prospect input (entity/operation/function/query keys) truthfully FAILED with PROSPECT_INPUT_FIELD_REJECTED (422); duplicate request within the TTL returned the existing SUCCEEDED execution (idempotent replay, no double execution) while a post-TTL retry created a new append-only record linked via parent_execution_id + correlation_id; the exec_concierge → read_own_readiness_assessment regression re-verified SUCCEEDED through the refactored chain with unchanged response shape. Inactive/disabled agent and tool kill switches, exact target/operation/scope/permission/risk gates, and cross-user/cross-organization isolation remain enforced by the same chain (client user_id/organization_id are never read; ownership is anchored to the server-resolved authenticated caller) — code-audit verified rather than live-triggered to preserve frozen registry values. Final executable Workforce agents: 2 (exec_concierge, growth_agent); final executable business tools: 2 (read_own_readiness_assessment, prospect_intelligence); no LLM is invoked by either tool; no external action exists in either path; the aiWorkforce compatibility fence, Phase 6 governed adapter paths, AgentApproval model, AIAgentState, AITask, and subscriptions are unchanged. Phase 8 Prospect and Lead Management definition deployed 2026-09-15 — the FIRST state-changing Workforce capability, implemented and tested but NOT activated: the Prospect entity was created with owner-anchored least-privilege RLS (create: own owner_user_id or platform roles only; read: owner + tenant org admin + platform admin/dev audit; update/delete: platform roles only, ordinary-user update and delete denied; no cross-user or cross-tenant access) and registered in the RLS Registry; the single new tool definition create_own_prospect is registered in AgentToolRegistry as DRAFT + enabled=false (ENTITY:Prospect, CREATE, user_scoped/self_records, risk medium, human_approval_required true, allow-list [growth_agent] only — no wildcards); the tool performs exactly ONE business operation — creating ONE Prospect record owned by the SERVER-resolved authenticated user with status forced NEW — and nothing else: no update, no delete, no status transition, no qualification mutation, no conversion, no CRM; the strict allow-list input contract (base44/shared/prospectCreate.ts) rejects unknown fields including client-supplied status, owner_user_id, organization_id, and entity/operation/function/query selectors, control characters, oversized values, non-bare-domain websites, non-integer scores, and malformed references; owner_user_id and organization_id are derived server-side and client values are never read; provenance is truthful — an intelligence_reference must resolve to a real SUCCEEDED server-issued prospect_intelligence execution owned by the same user before source=ai_workforce_prospect_intelligence is claimed, otherwise the truthful source ai_workforce_create_own_prospect is stored, and no EvidenceItem or external-verification claim is fabricated; the human approval gate was extended within the single existing AgentApproval mechanism in the shared Agent Orchestration Core: a create request without approval is QUEUED behind a NEW server-issued PENDING AgentApproval with no mutation before decision, a supplied approval_id is re-validated server-side on every field before any mutation (existence, user and capability match, server provenance, input-hash binding, APPROVED status, expiry, self-approval prohibition, and single-use consumption), rejected or mismatched approvals produce truthful BLOCKED audit records, and a successful create links execution and approval bidirectionally then consumes the approval after exactly one execution — approval never auto-executes and no hidden callbacks exist; no automatic chaining from prospect_intelligence to create was added — creation requires an explicit request plus approval; 18/18 deterministic validation tests pass against the exact shipped module (dual-runner suite committed at base44/shared/prospectCreate.test.ts); live DRAFT-state security tests passed (growth_agent requesting create_own_prospect is truthfully BLOCKED with TOOL_NOT_EXECUTABLE because the tool is DRAFT, claimed agent and tool mismatches BLOCKED with identity errors, and injected ownership fields are never read); while the tool remains DRAFT + disabled it has NO execution path through any boundary, and its medium risk exceeds the current low max_risk_level so activation additionally requires an explicit AgentOrchestrationConfig threshold decision; ZERO Prospect records exist in production; the full approval chain (pending creates no record, rejected creates no record, approved creates exactly one with server-derived ownership) is code-audit verified end-to-end and awaits live verification at activation; both existing live capabilities, the aiWorkforce fence, the AgentApproval decision flow, AIAgentState, AITask, subscriptions, and all existing governance are unchanged. Phase 8.1 activation deployed 2026-09-15: create_own_prospect was set ACTIVE + enabled in AgentToolRegistry (all other fields unchanged — ENTITY:Prospect CREATE, user_scoped/self_records, risk medium, human_approval_required true, allow-list [growth_agent] only) and AgentOrchestrationConfig max_risk_level was raised from low to medium (stop_new_executions remains false; no other medium/high/critical tool is ACTIVE+enabled — invoke_resolve_subscription remains DRAFT+disabled and no UPDATE or DELETE operation tool exists). One test-driven fix was added to the shared Agent Orchestration Core: request-time input validation inside the human-approval branch — an approval is now only ever issued for a payload that satisfies the tool input contract; injected or malformed create requests are refused BEFORE any approval record exists, with a truthful BLOCKED audit record. LIVE-VERIFIED end-to-end: growth_agent → prospect_intelligence for a legitimate company (Ayala Corporation) returned SUCCEEDED with OBSERVED/INFERRED/UNKNOWN labels and no external verification; an explicit create_own_prospect request referencing that real SUCCEEDED execution produced the correct PENDING_APPROVAL state — a NEW server-issued PENDING AgentApproval (approval_id 786e0b21-efa2-4830-b0c9-7ebbb2a03aea) plus a QUEUED AgentExecution (execution_id 09b83247-580d-45d7-a39a-547d6281751a), both server-issued (provenance server_orchestration), tied to the exact agent and tool, anchored to the server-resolved authenticated caller, bidirectionally linked by approval_id and correlation_id, bound to input hash 60a506a1, with the 24-hour expiry set and ZERO Prospect records created. Live negative tests passed: a self-approval attempt by the requester was BLOCKED with SELF_APPROVAL_PROHIBITED (the decision genuinely requires a second human approver, by design); reusing the pending approval with different input was BLOCKED with APPROVAL_INPUT_MISMATCH; a create request injecting owner_user_id, organization_id, status, and entity/operation selectors was refused at request time with PROSPECT_INPUT_FIELD_REJECTED (422) and created NO approval record; a claimed agent (exec_concierge) was BLOCKED with AGENT_IDENTITY_MISMATCH; claimed tools update_own_prospect and delete_own_prospect were BLOCKED with TOOL_IDENTITY_MISMATCH (no UPDATE/DELETE tool exists); a wrong execution_id lookup returned 404; the exec_concierge → read_own_readiness_assessment regression re-verified SUCCEEDED with unchanged response shape; spoofed body-level user_id/organization_id were never read — every record is anchored to the server-resolved caller. The positive approval decision (APPROVED → one governed re-request → exactly one Prospect created) remains PENDING human decision by a second approver account and is the only remaining step; single-use approval consumption, rejection, expiry enforcement, and decided-record immutability remain code-audit verified within the same single AgentApproval mechanism. The aiWorkforce compatibility fence, Phase 6 governed adapter paths, the AgentApproval decision flow, AIAgentState, AITask, subscriptions, and all existing governance are unchanged. Phase 8.2 read capability deployed and live-verified 2026-09-15: read_own_prospects was registered in AgentToolRegistry (ENTITY:Prospect READ, user_scoped/self_records, risk low, human_approval_required false, allow-list [growth_agent] only — no wildcards) and implemented as the fourth capability inside the single Agent Orchestration Core chain — no second execution path, no client-side Prospect access, no bypass of the core or AgentExecution, no InvokeLLM, no email, no CRM, no payments, no scheduling, no external calls. The tool filters Prospect exclusively by the SERVER-resolved owner_user_id of the authenticated caller, in fixed -created_date order, bounded to 20 records, accepting only an optional registered-status filter; input keys beyond that (user_id, organization_id, owner_user_id, filter, query, sort, limit, fields, entity, operation, selectors) are rejected by the strict contract (base44/shared/prospectRead.ts), and the fixed safe projection excludes internal record ids, ownership anchors, and tenant fields. 9/9 deterministic contract tests pass against the shipped module (dual-runner suite committed at base44/shared/prospectRead.test.ts). Live tests through agentOrchestrationService: a DRAFT-state request was truthfully BLOCKED with TOOL_NOT_EXECUTABLE before activation; after activating ONLY this tool (DRAFT+disabled → ACTIVE+enabled — growth_agent, prospect_intelligence, create_own_prospect, the medium risk threshold, and the pending Phase 8.1 approval were all untouched), an authenticated empty-state read SUCCEEDED with zero Prospect records, bounded_to 20, no error, and no mutation; body-level spoofed user_id/organization_id were ignored (identity is server-resolved only); a claimed agent (exec_concierge) was BLOCKED with AGENT_IDENTITY_MISMATCH; a claimed tool (read_own_user_profile) was BLOCKED with TOOL_IDENTITY_MISMATCH; injected input identity/selector/query/limit fields were truthfully FAILED with PROSPECT_READ_FIELD_REJECTED (422) and an unregistered status with PROSPECT_READ_STATUS_INVALID. Every live execution carries server provenance (source=agent_orchestration_service, metadata.provenance=server_orchestration) and a recorded correlation_id; no AgentApproval was required or created for this low-risk read. Final executable Workforce agents: 2 (exec_concierge, growth_agent); final executable business tools: 4 (read_own_readiness_assessment, prospect_intelligence, create_own_prospect, read_own_prospects); the five additional read-only ACTIVE registry definitions remain without any execution path in any boundary; no UPDATE or DELETE Prospect tool exists; invoke_resolve_subscription remains DRAFT+disabled.',
  },
  privacy: {
    status: 'PASS',
    evidence: 'Privacy framework verified comprehensive: privacyEngine.js defines a complete RA 10173 framework — 12-entity data inventory, 6 consent types, 7 DSR rights, 8 PIAs (one critical/approved), 8 retention policies, 7 identity-protection rules, 8 responsible-AI controls, 20-category regression suite (1209/1211 pass), 8-item evidence registry, 10/10 PASS certification, DPO command center. Privacy entities enforced: ConsentRecord (owner append-only RLS), DataSubjectRequest (7 rights, owner-scoped), AccountDeletionRequest (email-verified, grace-period, 9-action backend function), DeletionPolicyConfig (admin-managed per-plan grace). Consent lifecycle now fully operational: consentService.js manages authoritative ConsentRecord lifecycle (load, grant, withdraw, registration consent, telemetry sync); Register.jsx writes terms + privacy_policy consent after OTP verification; SubscriptionContext seeds consent state after auth and records registration consent for new OAuth accounts; MyPrivacy ConsentTab has functional grant/withdraw toggle switches for marketing, ai_personalization, analytics, and cookies. AI personalization consent enforced at all 5 production context injection paths (ai.js shouldInjectExecutiveContext, academyAi.js withCompany + profile fields, executiveActionEngine.js user context, promotionForecastEngine.js user name, executiveIdentityPresentations.js identity JSON) — fail-closed when consent absent; non-personalized AI remains functional. Analytics consent linked to authoritative store via _syncAnalyticsToTelemetry → setTelemetryConsent; null state preserves local opt-out, true/false overrides. All three audited privacy gaps (consent provenance, processing-level AI enforcement, analytics sync) are now closed. Data-protection controls (RLS, deletion, DSR, identity protection) remain genuinely enforced.',
  },
};

export const CONFIGURATION_DRIFT_AUDIT = [
  { configuration: 'Platform config version', currentValue: '2026-07-10-v1', approvedValue: '2026-07-10-v1', sourceOfTruth: 'manageConfig', drift: false, impact: 'None' },
  { configuration: 'Journey points and levels', currentValue: 'Matches frontend mirror', approvedValue: 'manageConfig baseline', sourceOfTruth: 'manageConfig', drift: false, impact: 'None' },
  { configuration: 'Readiness dimensions', currentValue: '12 matching dimensions', approvedValue: '12 approved dimensions', sourceOfTruth: 'manageConfig', drift: false, impact: 'None' },
  { configuration: 'Trust factors', currentValue: '11 matching factors', approvedValue: '11 approved factors', sourceOfTruth: 'manageConfig', drift: false, impact: 'None' },
];