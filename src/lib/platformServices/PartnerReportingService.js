export function exportPartnerReport(data, format = 'json') { if (format === 'pdf') { window.print(); return; } const partners = data.partners || []; let body; let type; if (format === 'csv') { const keys = ['partnerName', 'partnerType', 'partnerCategory', 'partnerStatus', 'partnershipStage', 'relationshipOwner', 'strategicFitScore', 'partnerHealthScore']; body = [keys.join(','), ...partners.map((p) => keys.map((k) => JSON.stringify(p[k] ?? '')).join(','))].join('\n'); type = 'text/csv'; } else { body = JSON.stringify(data, null, 2); type = 'application/json'; } const blob = new Blob([body], { type }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = `strategic-partner-report.${format === 'csv' ? 'csv' : 'json'}`; link.click(); URL.revokeObjectURL(url); }
export const PartnerReportingService = { exportPartnerReport };