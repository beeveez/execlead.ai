import React from 'react';

export default function ConfidenceAdjustments({ adjustments, bands }) {
  return <div className="grid grid-cols-1 lg:grid-cols-2 gap-4"><section className="rounded-2xl border border-white/8 bg-white/[0.02] p-5"><h2 className="text-sm font-semibold text-white mb-3">Confidence Threshold Adjustments</h2>{adjustments.map((item) => <div key={item.threshold} className="py-3 border-b border-white/5"><div className="flex justify-between text-xs"><span className="text-white/70">{item.threshold}</span><span className="text-amber-400">{item.current} → {item.recommended}</span></div><p className="text-[10px] text-white/35 mt-1">{item.reason}</p></div>)}</section><section className="rounded-2xl border border-white/8 bg-white/[0.02] p-5"><h2 className="text-sm font-semibold text-white mb-3">Reflection-Depth Sensitivity</h2>{bands.map((band) => <div key={band.key} className="flex items-center justify-between py-3 border-b border-white/5 text-xs"><span className="text-white/60">{band.label}</span><span className="text-indigo-300">{band.averageReadinessChange > 0 ? '+' : ''}{band.averageReadinessChange} readiness · {band.evidenceCount} signals</span></div>)}</section></div>;
}