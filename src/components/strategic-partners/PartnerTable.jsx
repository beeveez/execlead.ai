import React from 'react';
import { Link } from 'react-router-dom';
import { ChevronRight } from 'lucide-react';
import { categoryLabel } from '@/lib/strategic-partners/partnerConfig';
import { priorityLabel, relationshipClass, statusTone } from '@/lib/strategic-partners/partnerDisplay';

export default function PartnerTable({ partners, onEdit }) {
  return <div className="overflow-x-auto rounded-xl border border-border"><table className="w-full min-w-[1120px] text-sm"><thead className="bg-muted/40 text-left text-xs text-muted-foreground"><tr><th className="p-3">Partner</th><th>Category</th><th>Strategic Role</th><th>Priority</th><th>Stage</th><th>Strategic Fit</th><th>Commercial Potential</th><th>Next Action</th><th>Status</th><th>Actions</th></tr></thead><tbody className="divide-y divide-border">{partners.map((partner) => <tr key={partner.id}><td className="p-3 font-medium text-foreground">{partner.partnerName}</td><td className="text-muted-foreground">{categoryLabel(partner.partnerCategory)}</td><td className="max-w-56 text-muted-foreground">{partner.valueProposition || '—'}</td><td>{priorityLabel(partner)}</td><td>{partner.partnershipStage}</td><td>{partner.strategicFitScore ?? '—'}</td><td>{partner.commercialPotentialScore ?? '—'}</td><td>{partner.nextAction || 'Define outreach plan'}</td><td><span className={`inline-flex rounded-full border px-2 py-1 text-[10px] ${statusTone(partner)}`}>{relationshipClass(partner)}</span></td><td><button onClick={() => onEdit(partner)} className="min-h-11 px-2 text-primary">Edit</button><Link to={`/operations/strategic-partners/${partner.id}`} className="inline-flex min-h-11 min-w-11 items-center justify-center" aria-label={`Open ${partner.partnerName}`}><ChevronRight size={16}/></Link></td></tr>)}</tbody></table></div>;
}