import { Link } from "react-router-dom";

export default function SuccessionBenchPanel({ candidates }) {
  const focus = candidates.filter((candidate) => ["Maria Santos", "Jonathan Cruz", "Erica Molina"].includes(candidate.employeeName));
  return <section className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-semibold uppercase tracking-widest text-cyan-600">Primary Role Focus</p><h2 className="mt-1 text-xl font-bold text-card-foreground">Director, Customer Operations</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="text-xs text-muted-foreground"><tr><th className="pb-3">Candidate</th><th>Readiness</th><th>Evidence Strength</th><th>Succession Risk</th><th></th></tr></thead><tbody>{focus.map((candidate) => <tr key={candidate.id} className="border-t border-border"><td className="py-3 font-semibold text-foreground">{candidate.employeeName}</td><td>{candidate.readinessScore}</td><td>{candidate.evidenceStrength}</td><td className="capitalize">{candidate.successionRisk}</td><td><Link className="text-xs font-semibold text-cyan-600" to={`/enterprise/candidate/${candidate.id}`}>Open Development Plan</Link></td></tr>)}</tbody></table></div><div className="mt-5 rounded-xl border border-amber-500/30 bg-amber-500/10 p-4 text-sm text-amber-800 dark:text-amber-200">Only one candidate currently exceeds the organization’s executive-readiness threshold for this role. Additional accelerated development actions are recommended for the remaining bench.</div></section>;
}