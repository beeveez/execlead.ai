import React, { useState } from 'react';
const CATEGORIES = ['Overproduction', 'Waiting', 'Transport', 'Overprocessing', 'Inventory', 'Motion', 'Defects', 'Unused Talent'];
export default function WastePanel({ hotspots = [], projects = [], onSubmit }) {
  const [category, setCategory] = useState('Waiting'); const [title, setTitle] = useState(''); const [description, setDescription] = useState(''); const [projectId, setProjectId] = useState('');
  const submit = async (event) => { event.preventDefault(); await onSubmit({ category, title, description, projectId, priority: 'medium' }); setTitle(''); setDescription(''); };
  return <section className="grid gap-4 lg:grid-cols-2"><div className="rounded-xl border border-white/5 bg-white/[0.02] p-5"><h2 className="text-sm font-semibold text-white">Lean Waste Hotspots</h2><div className="mt-4 grid grid-cols-2 gap-2">{CATEGORIES.map((item) => { const count = hotspots.find((hotspot) => hotspot.category === item)?.count || 0; return <div key={item} className="flex justify-between rounded-lg bg-white/[0.03] px-3 py-2 text-xs"><span className="text-white/55">{item}</span><span className={count ? 'text-amber-400' : 'text-white/20'}>{count}</span></div>; })}</div></div><form onSubmit={submit} className="rounded-xl border border-white/5 bg-white/[0.02] p-5"><h2 className="text-sm font-semibold text-white">Tag Friction or Opportunity</h2><div className="mt-4 space-y-3"><select value={category} onChange={(e) => setCategory(e.target.value)} className="form-input">{CATEGORIES.map((item) => <option key={item}>{item}</option>)}</select><input required value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Friction point" className="form-input" /><textarea required value={description} onChange={(e) => setDescription(e.target.value)} placeholder="Observed impact" className="form-input" /><select value={projectId} onChange={(e) => setProjectId(e.target.value)} className="form-input"><option value="">No linked project</option>{projects.map((project) => <option key={project.id} value={project.projectId}>{project.title}</option>)}</select></div><button className="mt-4 rounded-lg bg-amber-500 px-4 py-2 text-sm font-semibold text-white">Tag waste</button></form></section>;
}