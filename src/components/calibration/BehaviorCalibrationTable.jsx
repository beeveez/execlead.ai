import React from 'react';
import { TrendingDown, Minus, TrendingUp } from 'lucide-react';

export default function BehaviorCalibrationTable({ behaviors }) {
  return <section className="rounded-2xl border border-white/8 bg-white/[0.02] p-5 overflow-x-auto"><h2 className="text-sm font-semibold text-white mb-4">Behavior Correlation Stability</h2><table className="w-full min-w-[680px] text-xs"><thead><tr className="text-left text-[10px] uppercase tracking-wider text-white/30 border-b border-white/8"><th className="pb-3">Behavior</th><th className="pb-3">Evidence</th><th className="pb-3">Confidence</th><th className="pb-3">Stability</th><th className="pb-3">Predictive value</th><th className="pb-3">Trend</th></tr></thead><tbody>{behaviors.map((item) => { const Trend = item.predictiveChange < 0 ? TrendingDown : item.predictiveChange > 0 ? TrendingUp : Minus; return <tr key={item.key} className="border-b border-white/5"><td className="py-3 text-white/75">{item.label}</td><td className="py-3 text-white/50">{item.evidenceCount}</td><td className="py-3 text-white/50">{item.averageConfidence}%</td><td className="py-3 text-white/50">{item.stability}%</td><td className="py-3 text-white/50">{item.predictiveValue > 0 ? '+' : ''}{item.predictiveValue}</td><td className={`py-3 ${item.predictiveChange < 0 ? 'text-rose-400' : item.predictiveChange > 0 ? 'text-emerald-400' : 'text-white/30'}`}><Trend size={13} className="inline mr-1" />{item.predictiveChange}</td></tr>; })}</tbody></table></section>;
}