{
  "name": "ExternalDeliveryConnectorRegistry",
  "type": "object",
  "description": "Provider-neutral external delivery connector registry. Configuration/architecture only \u2014 a registry record authorizes nothing: the Agent Orchestration Core remains the single authorization authority, and a connector may only be invoked by the governed outreach execution boundary with an already-authorized execution context. Phase 12 registers exactly one ACTIVE connector: the non-delivering Sandbox Delivery Connector. Phase 13 adds exactly one DRAFT architecture record for the Google Workspace Gmail provider (not connected, not enabled, no credentials).",
  "properties": {
    "connector_id": {
      "type": "string",
      "description": "Unique authoritative connector identifier (single source of truth). Wildcards are prohibited."
    },
    "name": {
      "type": "string",
      "description": "Display name of the delivery connector"
    },
    "connector_type": {
      "type": "string",
      "enum": [
        "SANDBOX",
        "GOOGLE_WORKSPACE_GMAIL",
        "EMAIL_PROVIDER",
        "MESSAGING_PROVIDER",
        "VOICE_PROVIDER",
        "SOCIAL_PROVIDER",
        "CRM_PROVIDER",
        "OTHER"
      ],
      "description": "Provider-neutral connector classification. SANDBOX = non-delivering contract-validation connector. GOOGLE_WORKSPACE_GMAIL = Phase 13 architecture-only Gmail provider adapter (DRAFT, not connected)."
    },
    "version": {
      "type": "string",
      "default": "1.0.0",
      "description": "Connector definition version"
    },
    "status": {
      "type": "string",
      "enum": [
        "ACTIVE",
        "DRAFT",
        "PAUSED",
        "RETIRED"
      ],
      "default": "DRAFT",
      "description": "Registry lifecycle: ACTIVE (registered and referenceable by the delivery contract), DRAFT (defined, not referenceable), PAUSED (temporarily suspended), RETIRED (removed from service)"
    },
    "enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether the connector may be referenced by the delivery architecture. A registry record NEVER authorizes execution on its own."
    },
    "supported_channels": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "EMAIL",
          "LINKEDIN",
          "CALL"
        ]
      },
      "description": "Provider-neutral channels this connector can conceptually map. No channel is contacted by the sandbox connector, and the Phase 13 Google connector is not connected."
    },
    "supports_delivery": {
      "type": "boolean",
      "default": false,
      "description": "Whether the connector performs real delivery. FALSE for the sandbox connector and FALSE for the Phase 13 Google architecture connector \u2014 neither transmits anything."
    },
    "sandbox_only": {
      "type": "boolean",
      "default": true,
      "description": "Whether the connector is restricted to non-delivering sandbox behavior"
    },
    "requires_human_approval": {
      "type": "boolean",
      "default": true,
      "description": "Whether deliveries through this connector require the governed human approval of the outreach execution boundary. Delivery connectors may never weaken this."
    },
    "delivery_identity": {
      "type": "string",
      "maxLength": 320,
      "description": "Fixed server-controlled sender/delivery identity for this connector (e.g. the dedicated Growth mailbox for the Gmail provider). Server configuration only \u2014 never client-supplied, never dynamically configurable by an agent or client."
    },
    "source": {
      "type": "string",
      "enum": [
        "production_catalog",
        "platform_config"
      ],
      "default": "production_catalog",
      "description": "Provenance of the connector definition"
    },
    "description": {
      "type": "string",
      "maxLength": 2000,
      "description": "What the connector does, what it never does, and its current usage status"
    }
  },
  "required": [
    "connector_id",
    "name",
    "connector_type",
    "status",
    "supported_channels",
    "supports_delivery",
    "sandbox_only"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "user_condition": {
            "role": "platform_admin"
          }
        },
        {
          "user_condition": {
            "role": "founder_root_admin"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "developer"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "user_condition": {
            "role": "platform_admin"
          }
        },
        {
          "user_condition": {
            "role": "founder_root_admin"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "developer"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "user_condition": {
            "role": "platform_admin"
          }
        },
        {
          "user_condition": {
            "role": "founder_root_admin"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "developer"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "user_condition": {
            "role": "platform_admin"
          }
        },
        {
          "user_condition": {
            "role": "founder_root_admin"
          }
        }
      ]
    }
  }
}