import { CheckCircle2, AlertTriangle } from 'lucide-react';
export default function QualityPanel({ item }) {
  const checks = item.quality_checks || {};
  return <aside className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><h3 className="font-semibold text-foreground">Content Quality</h3><span className="text-2xl font-bold text-foreground">{item.ai_quality_score}/100</span></div><div className="mt-4 space-y-2">{Object.entries(checks).map(([key,value])=><div key={key} className="flex items-center justify-between text-xs"><span className="capitalize text-muted-foreground">{key.replaceAll('_',' ')}</span><span className="flex items-center gap-1 text-emerald-600"><CheckCircle2 className="h-3.5 w-3.5"/>{value}</span></div>)}</div><div className="mt-4 border-t border-border pt-4">{item.potential_issues?.length ? item.potential_issues.map(issue=><p key={issue} className="flex gap-2 text-xs text-amber-600"><AlertTriangle className="h-4 w-4 shrink-0"/>{issue}</p>) : <p className="text-xs text-emerald-600">Potential issues: None</p>}</div><div className="mt-4 text-xs text-muted-foreground"><p className="font-semibold text-foreground">Sources</p>{item.source_references?.map(source=><p key={source}>• {source}</p>)}</div></aside>;
}