const LEVELS = ["High", "Medium", "Low"];
const colors = { "High-High": "bg-emerald-500/20", "High-Medium": "bg-cyan-500/20", "Medium-High": "bg-cyan-500/20", "Low-Low": "bg-destructive/10" };

export default function SuccessionMatrix({ snapshot, candidates }) {
  const matrix = snapshot.readinessDistribution?.nineBox || {};
  return <div className="grid gap-5 xl:grid-cols-[1fr_360px]"><section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-semibold text-card-foreground">9-Box Successor Matrix™</h2><p className="mt-1 text-xs text-muted-foreground">Candidate placement is based on calibrated evidence strength.</p><div className="mt-4 grid grid-cols-3 gap-2">{LEVELS.flatMap((performance) => LEVELS.map((potential) => { const key = `${performance}-${potential}`; return <div key={key} className={`min-h-28 rounded-xl border border-border p-3 ${colors[key] || "bg-muted"}`}><p className="text-[10px] uppercase text-muted-foreground">{performance} performance</p><p className="text-xs text-foreground">{potential} potential</p><p className="mt-4 text-2xl font-bold text-foreground">{matrix[key] || 0}</p></div>; }))}</div></section><section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-semibold text-card-foreground">Strongest Evidence</h2><div className="mt-3 space-y-3">{candidates.slice(0, 8).map((candidate, index) => <div key={candidate.id} className="flex items-center justify-between border-b border-border pb-3 text-sm"><div><p className="font-medium text-foreground">{index + 1}. {candidate.recommendedDevelopmentActions?.displayName}</p><p className="text-xs text-muted-foreground">{candidate.targetRole}</p></div><span className="font-bold text-cyan-600">{candidate.evidenceStrength}</span></div>)}</div></section></div>;
}