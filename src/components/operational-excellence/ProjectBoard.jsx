import React from 'react';
import { ArrowRight, CheckCircle2 } from 'lucide-react';
export default function ProjectBoard({ projects = [], analyses = [], onMeasure, onAnalyze, onApprove, onGenerate }) {
  return <section><h2 className="mb-4 text-sm font-semibold text-white">Active Improvement Projects</h2><div className="space-y-3">{projects.length === 0 && <div className="rounded-xl border border-dashed border-white/10 p-6 text-sm text-white/35">No active improvement projects.</div>}{projects.map((project) => { const analysis = analyses.find((item) => item.projectId === project.projectId); return <div key={project.id} className="rounded-xl border border-white/5 bg-white/[0.02] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><span className="text-sm font-semibold text-white">{project.title}</span><span className="rounded-full bg-indigo-500/10 px-2 py-0.5 text-[10px] font-bold text-indigo-400">{project.status}</span></div><p className="mt-1 max-w-3xl text-xs text-white/45">{project.problemStatement}</p><p className="mt-2 text-[11px] text-white/30">{project.projectId} · KPI: {project.targetKPI} · {project.baselineValue} → {project.targetValue} · Owner: {project.owner}</p></div><div className="flex gap-2">{project.status === 'Define' && <button onClick={() => onMeasure(project)} className="rounded-lg border border-white/10 px-3 py-2 text-xs text-white/70">Start Measure</button>}{project.status === 'Measure' && !analysis && <button onClick={() => onAnalyze(project)} className="rounded-lg bg-cyan-500 px-3 py-2 text-xs font-semibold text-white">Analyze <ArrowRight size={12} className="ml-1 inline" /></button>}{analysis?.approvalStatus === 'pending' && <button onClick={() => onApprove(analysis)} className="rounded-lg bg-amber-500 px-3 py-2 text-xs font-semibold text-white">Approve RCA</button>}{analysis?.approvalStatus === 'approved' && project.status === 'Analyze' && <button onClick={() => onGenerate(analysis)} className="rounded-lg bg-emerald-500 px-3 py-2 text-xs font-semibold text-white">Create actions</button>}{['Improve', 'Control', 'Completed'].includes(project.status) && <CheckCircle2 size={18} className="text-emerald-400" />}</div></div></div>; })}</div></section>;
}