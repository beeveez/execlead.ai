import React from 'react';

export default function PredictorStabilityReport({ stability, paths }) {
  return <div className="grid grid-cols-1 lg:grid-cols-2 gap-4"><section className="rounded-2xl border border-white/8 bg-white/[0.02] p-5"><h2 className="text-sm font-semibold text-white mb-4">Readiness Predictor Stability Report</h2><div className="grid grid-cols-2 gap-3">{[['Paired members', stability.pairedMembers], ['Simulation gain', stability.averageSimulationGain], ['Readiness change', stability.averageReadinessChange], ['Correlation', stability.correlation], ['Directional alignment', `${stability.directionalAlignment}%`], ['Status', stability.status]].map(([label, value]) => <div key={label} className="rounded-lg bg-white/[0.03] p-3"><div className="text-sm font-semibold text-white">{value}</div><div className="text-[9px] text-white/35">{label}</div></div>)}</div></section><section className="rounded-2xl border border-white/8 bg-white/[0.02] p-5"><h2 className="text-sm font-semibold text-white mb-4">Recommendation Completion by Path</h2>{paths.length ? paths.map((item) => <div key={item.path} className="flex justify-between py-3 border-b border-white/5 text-xs"><span className="capitalize text-white/60">{item.path}</span><span className="text-emerald-400">{item.completionRate}% · {item.completed}/{item.recommended}</span></div>) : <p className="text-xs text-white/40">No path-level recommendation evidence yet.</p>}</section></div>;
}