import React, { useState } from 'react';
export default function RootCauseForm({ project, onSubmit, onCancel }) {
  const [method, setMethod] = useState('5 Whys'); const [rootCause, setRootCause] = useState(''); const [symptoms, setSymptoms] = useState(''); const [actions, setActions] = useState(''); const [confidence, setConfidence] = useState(80); const [saving, setSaving] = useState(false);
  const submit = async (event) => { event.preventDefault(); setSaving(true); try { await onSubmit({ projectId: project.projectId, method, rootCause, symptoms, confidence: Number(confidence), evidence: [], recommendedActions: actions.split('\n').map((item) => item.trim()).filter(Boolean) }); } finally { setSaving(false); } };
  return <form onSubmit={submit} className="rounded-2xl border border-cyan-500/20 bg-cyan-500/5 p-5"><h2 className="text-sm font-semibold text-white">Root Cause Analysis · {project.title}</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><select value={method} onChange={(e) => setMethod(e.target.value)} className="form-input"><option>5 Whys</option><option>Fishbone</option><option>Pareto</option><option>FMEA</option></select><input type="number" min="0" max="100" value={confidence} onChange={(e) => setConfidence(e.target.value)} className="form-input" aria-label="Confidence percent" /><textarea value={symptoms} onChange={(e) => setSymptoms(e.target.value)} placeholder="Observed symptoms" className="form-input md:col-span-2" /><textarea required value={rootCause} onChange={(e) => setRootCause(e.target.value)} placeholder="Verified root cause" className="form-input md:col-span-2" /><textarea required value={actions} onChange={(e) => setActions(e.target.value)} placeholder="Recommended actions, one per line" className="form-input md:col-span-2" /></div><div className="mt-4 flex gap-2"><button disabled={saving} className="rounded-lg bg-cyan-500 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">Save analysis</button><button type="button" onClick={onCancel} className="rounded-lg border border-white/10 px-4 py-2 text-sm text-white/60">Cancel</button></div></form>;
}