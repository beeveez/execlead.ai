{
  "name": "UsageLog",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "maxLength": 1000
    },
    "module": {
      "type": "string",
      "enum": [
        "coach",
        "simulator",
        "challenge",
        "debate",
        "academy",
        "companies",
        "career",
        "metrics",
        "resume",
        "council",
        "legacy",
        "other"
      ]
    },
    "tokens_estimated": {
      "type": "number",
      "default": 0
    },
    "input_tokens": {
      "type": "number",
      "default": 0,
      "description": "Estimated prompt/input tokens"
    },
    "output_tokens": {
      "type": "number",
      "default": 0,
      "description": "Estimated completion/output tokens"
    },
    "cost_estimated": {
      "type": "number",
      "default": 0
    },
    "model": {
      "type": "string"
    },
    "provider": {
      "type": "string",
      "enum": [
        "openai",
        "anthropic",
        "google",
        "deepseek",
        "meta",
        "platform",
        "unknown"
      ],
      "default": "unknown",
      "description": "AI provider derived from the model name"
    },
    "response_time_ms": {
      "type": "number",
      "default": 0,
      "description": "End-to-end response latency in milliseconds"
    },
    "status": {
      "type": "string",
      "enum": [
        "success",
        "error",
        "timeout",
        "rate_limited",
        "network_error",
        "validation_error"
      ],
      "default": "success"
    },
    "error_type": {
      "type": "string",
      "description": "Short error classification when status != success"
    },
    "prompt_id": {
      "type": "string",
      "description": "Optional correlation ID for a specific prompt/request"
    },
    "organization_id": {
      "type": "string",
      "description": "Organization scope for enterprise analytics"
    },
    "organization_name": {
      "type": "string"
    },
    "user_name": {
      "type": "string",
      "description": "Denormalized user display name for top-users reporting"
    },
    "direct_attempted_provider": {
      "type": "string",
      "description": "Direct AI provider attempted for this generation ('direct_google_gemini'), null when no direct attempt was made"
    },
    "direct_attempt_status": {
      "type": "string",
      "enum": [
        "success",
        "failed"
      ],
      "description": "Outcome of the direct provider attempt: success (response produced directly) or failed (governed fallback used)"
    },
    "direct_error_category": {
      "type": "string",
      "description": "Sanitized direct-provider error category on a failed direct attempt (e.g. rate_limited, model_not_found, timeout). Never contains keys, authorization headers, credentials, or raw provider error bodies."
    },
    "fallback_used": {
      "type": "boolean",
      "description": "Whether the delivered response was produced by the Base44-managed fallback after a direct provider failure"
    },
    "final_executed_provider": {
      "type": "string",
      "description": "Provider that produced the delivered response: 'direct_google_gemini' or 'base44_managed_llm'"
    }
  },
  "required": [
    "module"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "platform_admin"
      }
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "user_condition": {
            "role": "platform_admin"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "developer"
          }
        },
        {
          "user_condition": {
            "role": "founder_root_admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "__immutable__"
      }
    },
    "delete": {
      "user_condition": {
        "role": "__immutable__"
      }
    }
  }
}