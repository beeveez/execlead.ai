const BUCKETS = ["emerging", "operational", "strategic", "executive"];
const LABELS = { emerging: "Emerging", operational: "Operational", strategic: "Strategic", executive: "Executive" };
const tone = (value) => value >= 15 ? "bg-cyan-500/25 text-cyan-700 dark:text-cyan-300" : value >= 8 ? "bg-cyan-500/15 text-cyan-700 dark:text-cyan-300" : "bg-muted text-muted-foreground";

export default function ReadinessHeatmap({ snapshot }) {
  const units = snapshot.readinessDistribution?.businessUnits || [];
  return <section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-semibold text-card-foreground">Readiness Heatmap™</h2><p className="mt-1 text-xs text-muted-foreground">Evidence-calibrated leaders by business unit</p><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[560px] text-sm"><thead><tr className="text-left text-xs text-muted-foreground"><th className="pb-3">Business Unit</th>{BUCKETS.map((bucket) => <th key={bucket} className="pb-3 text-center">{LABELS[bucket]}</th>)}</tr></thead><tbody>{units.map((unit) => <tr key={unit.name} className="border-t border-border"><td className="py-3 font-medium text-foreground">{unit.name}</td>{BUCKETS.map((bucket) => <td key={bucket} className="p-1.5 text-center"><span className={`block rounded-lg px-3 py-2 font-semibold ${tone(unit[bucket])}`}>{unit[bucket]}</span></td>)}</tr>)}</tbody></table></div></section>;
}